const toCsvText = (array) => array.map((row) => row.join(",")).join("\n");

// too easy because of the javascript functions, that i don't know how to use yet

const toCsv = (array) => array.join("\n");
